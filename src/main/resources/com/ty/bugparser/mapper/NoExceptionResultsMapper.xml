<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ty.bugparser.mapper.NoExceptionResultsMapper">

    <select id="queryAllSuspiciousResults" resultType="NoExceptionResults">
        SELECT * FROM FilteredNoExceptionResults
    </select>

    <select id="queryAllAnalysed" resultType="NoExceptionResults">
        SELECT * FROM FilteredNoExceptionResults WHERE assignee IS NOT NULL
    </select>

    <select id="queryAllNoAnalysed" resultType="NoExceptionResults">
        SELECT * FROM FilteredNoExceptionResults WHERE assignee IS NULL
    </select>

    <select id="queryRandomNoAnalysedSuspiciousId" resultType="_int">
        SELECT id, output_id
        FROM FilteredNoExceptionResults
        WHERE assignee IS NULL
        ORDER BY RANDOM()
        LIMIT 1
    </select>

    <select id="queryHarnessIdBySuspiciousId" resultType="_int">
        SELECT output_id FROM FilteredNoExceptionResults WHERE id = #{suspiciousId}
    </select>

    <select id="queryTestcaseIdByHarnessId" resultType="_int">
        SELECT testcase FROM HarnessResults WHERE id = #{harnessId}
    </select>

    <select id="queryTestcaseCodeByTestcaseId" resultType="string">
        SELECT testcase FROM Testcases WHERE id = #{testcaseId}
    </select>

    <select id="querySuspiciousResultsById" resultType="NoExceptionResults">
        SELECT * FROM FilteredNoExceptionResults WHERE id = #{id}
    </select>

    <update id="updateSuspiciousResults" parameterType="NoExceptionResults">
        UPDATE FilteredNoExceptionResults
        SET bugType=#{bugType},
            `engine`=#{engine},
            output_id=#{output_id},
            bugLabel=#{bugLabel},
            reason=#{reason},
            remarks=#{remarks},
            assignee=#{assignee},
            submit_date=#{submit_date}
        WHERE id=#{id}
    </update>


</mapper>